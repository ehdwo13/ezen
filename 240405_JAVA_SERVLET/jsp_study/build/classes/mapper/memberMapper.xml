<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="MemberMapper">
  	<insert id="new">
  		insert into member(id, password, email, age) values (#{id}, #{password}, #{email}, #{age})
  	</insert>
  	<select id="login" resultType="domain.MemberVO">
  		select * from member where id = #{id}
  	</select>
  	<update id="upd">
  		update member set password = #{password}, email = #{email}, age = #{age} where id = #{id}
  	</update>
  	<update id="ll">
  		update member set lastlogin = now() where id = #{id}
  	</update>
  	<delete id="del">
  		delete from member where id = #{id}
  	</delete>
  </mapper>